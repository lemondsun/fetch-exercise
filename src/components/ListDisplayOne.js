import React from 'react'

export default function ListDisplayOne(props) {
  return (
    <div>
      {//props.list.map((x) => (
      //  console.log(props.list)
        //))
      }
    </div>
  )
}
